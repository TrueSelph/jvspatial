"""
Test package for jvspatial authentication system.

This package contains comprehensive tests for all authentication components:
- Entities (User, APIKey, Session)
- Middleware (AuthenticationMiddleware, rate limiting, JWT)
- Decorators (auth_endpoint, admin decorators)
- Endpoints (registration, login, profile, API keys, admin functions)
- Integration (full auth flows and server integration)
"""
